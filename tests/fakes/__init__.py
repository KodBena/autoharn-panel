"""tests.fakes -- in-memory fakes structurally satisfying this backend's `Protocol` ports
(`backend/core/ports.py` and any sibling extension-level port), for tests that need real
predecessor/successor-walk and validation semantics without a live Postgres connection.
"""
